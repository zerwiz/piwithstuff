# Security Validation for testing

security:
  checks:
    - input_validation: "All inputs sanitized"
    - auth_required: "Authentication enforced"
    - sql_injection: "Prevention enabled"
    - cors_headers: "CORS configured"
    - secure_defaults: "Security defaults applied"
  status: valid

